import SwiftUI

public extension Color {
    static let brand = Color(red: 1.0, green: 0.84, blue: 0.0)
    static let onBrand = Color.black
    static let chipBackground = Color.secondary.opacity(0.15)
    static let imagePlaceholder = Color.secondary.opacity(0.2)
    static let shadowOverlay = Color.black.opacity(0.35)
    static let contentSecondary = Color.secondary
}
